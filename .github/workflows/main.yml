name: Fisrt-Job
on:  push
jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
     - name: checkout github code
       uses: actions/checkout@v4
     - name: maven 
       run: mvn test
     - name: checkstyle
       run: mvn chekcstyle:checkstyle